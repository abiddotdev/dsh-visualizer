import type { ModuleGuide } from '../types.ts'

/** Chart artifact guidance. */
export const chart: ModuleGuide = {
  module: 'chart',
  summary: 'Quantitative charts and graphs. Load Chart.js from an allowlisted CDN, initialize it in a script placed last, and hardcode data colors.',
}
